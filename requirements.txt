# Runtime-зависимости ядра фреймворка Pippa.
#
# Базовые формулы используют только стандартную библиотеку Python
# (math, dataclasses), поэтому внешних runtime-зависимостей нет.
#
# При добавлении численных симуляций (например, кривые вращения SPARC)
# раскомментируйте нужное:
# numpy>=1.24
# scipy>=1.10
# matplotlib>=3.7
